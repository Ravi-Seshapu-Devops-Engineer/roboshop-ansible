- name: configuring frontend
  hosts: frontend
  become: true
  tasks:
  - name: enable nginx 1.2.4
    ansible.builtin.command: dnf module enable nginx:1.24

  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: present